if (interactive()) {
  suppressMessages(require(usethis))
  suppressMessages(require(conflicted))
  suppressMessages(require(devtools))
  suppressMessages(require(testthat))
  suppressMessages(require(cyclocomp))
}
